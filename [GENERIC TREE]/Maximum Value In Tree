public static int max(Node node) {
      int maxNode = 0;
    for(int i = 0; i<node.children.size(); i++){
        int currNode = max(node.children.get(i));
        maxNode = Math.max(maxNode,currNode);
    }
    int m = Math.max(maxNode,node.data);
    return m;
    //return maxNode;
  }
